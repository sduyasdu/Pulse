# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Cloud Functions build output
functions/lib

# Claude Code scratch worktrees — each is a full checkout (node_modules and all).
# Only this subdirectory: .claude/skills/ is tracked and shared.
.claude/worktrees/

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
*.tsbuildinfo

# Service account keys — full project admin in a single file. Admin scripts
# (functions/scripts/) need one; it must never reach a commit. Keep it outside
# the repo if you can, and delete it when the script is done.
*serviceAccount*.json
*service-account*.json
*-adminsdk-*.json
gcp-key*.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
